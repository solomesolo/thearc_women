"""
Select primary (and optional secondary) lens from systems and flags.
"""

from engine.types import LensResult


def select_lens(systems, derived_flags, registry, config):
    """Return LensResult. Stub: return default LENS_BASELINE."""
    return LensResult()
