# Image placeholders

Temporary assets used until final photography/textures are ready.

| Path | Used on | Final asset spec |
|------|--------|-------------------|
| `system-bio-texture.svg` / `.jpg` | /system hero (background behind diagram) | Min 1600px, 4:3 or 3:2, desaturated/monochrome |
| `system-cell-structure.svg` / `.jpg` | /system Health Memory section (right column) | 600×800, biological/cellular abstract |
| `lab-report-detail.svg` / `.jpg` | /lab hero (right column) | 520×380, clinical/lab detail, clean neutral |

The app currently uses the `.svg` placeholders for a scientific texture look. When you have final assets, replace the `.jpg` files and update the image path constants in the page components to use `.jpg`.
